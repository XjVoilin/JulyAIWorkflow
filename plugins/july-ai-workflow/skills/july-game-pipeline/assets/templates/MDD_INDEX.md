# {产品名} — MDD 索引

> 版本：v1.0 | 日期：YYYY-MM-DD | 状态：draft
> 上游：`GDD.md` vX.Y，`QA_GDD.md` PASS

- Architecture Gate：PASS / BLOCKED
- Framework Gate：PASS / BLOCKED
- Generation Input Boundary：PASS / BLOCKED
- 复杂度：small / medium / complex
- 参考项目：none / <project-relative-or-user-specified-path>
- 资源清单：none / 资源清单.md

## 生成输入边界

- 允许输入：策划案、已批准 GDD/审查、目标工程源码与精确 pin、用户明确指定的参考项目、公用流程规则。
- 排除输入：被判无效的旧 MDD、旧模块数量/命名/目录/接口及其派生总结。
- 重生成方式：先记录缺陷与流程修正，再删除旧合同；从允许输入重新推导，不在旧文档上改写。

## 复杂度判定

| 维度 | 规模/数量 | 技术影响 |
|---|---:|---|
| 游戏状态与核心用例 |  |  |
| 持久化聚合 |  |  |
| 配置内容族 |  |  |
| UI/场景流 |  |  |
| 平台 seam |  |  |
| Editor/离线工具 |  |  |

- 文档深度选择：说明为什么当前模块粒度足以覆盖这些变化轴。

## 已验证项目与框架证据

| 能力 | manifest pin/工程位置 | 已核实的公开类型或入口 | 证据来源 | MDD 用途 |
|---|---|---|---|---|
| JulyArch |  |  | 精确 tag 源码/目标工程代码 |  |

只记录亲自读取过的精确 pin 源码和目标工程组合方式。不要把“包已安装”写成“接口已验证”。

## 精确调用证据

| 消费场景 | package@pin | 源码路径 | 完整类型/成员签名或枚举成员 | 成功语义 | 消费者可观察的失败/恢复通道 | 结论 |
|---|---|---|---|---|---|---|
|  |  |  |  |  |  | pass/gap |

本节只保存组合根、跨模块和其他不属于单一能力模块的调用证据。模块专属调用由对应模块文档持有，索引只引用其结论，不复制签名。方法名或日志不算合同；GDD 要求区分的失败必须能通过公开结果、事件或状态被产品观察。

## 参考项目校准

- 明确排除范围：`<excluded-scope-pattern>` 或 `none`
- 读取范围：组合根、能力模块、Views、Luban、asmdef、Editor、测试

| 参考证据 | 范围 | 结论 | 当前设计影响 |
|---|---|---|---|
|  | included/excluded | adopt/reject | 采用、改造或明确不采用 |

仅当顶部“参考项目”不是 `none` 时保留本节，并同时记录采纳与拒绝项。参考项目只能证明项目级模式，具体 July 成员仍以目标 pin 为准。

## 框架能力充足性审计

| GDD 所需基础能力 | 当前 July package/pin | 现有合同是否满足 | 责任判定 | 处置 |
|---|---|---|---|---|
|  |  | pass/fail | 产品拥有 / July Framework 缺失 | 产品模块 / `FG-*.md` 并阻塞 |

- `fail` 且判定为 July Framework 缺失时，不得继续补齐 MDD 或设计产品 workaround；先执行框架能力缺口 Gate。
- 判定为产品拥有时，说明为何它是玩法、内容、组合、Provider 选择或产品/平台 adapter，而不是可复用框架合同。

## 能力树与模块边界

用树形结构展示产品能力 → 模块。模块按稳定业务能力命名，不按波次、页面大类或技术层命名。组合根、生成代码、构建流程和发布清单放在拓扑章节，不计作能力模块。

## 变化场景 locality

| 真实变化场景 | 主要拥有模块 | 允许的附加落点 | 实际涉及模块/目录 | 结论 |
|---|---|---|---|---|
|  |  | composition/config/resource |  | pass/fail |

至少覆盖核心玩法规则变化、一个外围产品能力变化和一个平台/持久化变化。普通变化若因 Store/System/Procedure/View 技术分层而横跨三个以上能力模块，必须重新深化模块。

## 模块

| 编号 | 模块 | 深模块 Interface | JulyArch 角色 | 依赖 | 波次 | 文档 |
|---|---|---|---|---|---|---|
| M1 |  |  | Store/System/Procedure/View/普通类 | — | W1 |  |

## JulyArch 拓扑

### 组合根（不计作能力模块）

| 组合文件 | 只允许责任 | 注册/配置对象 | 禁止进入的行为 |
|---|---|---|---|
| `HotUpdateRegistrar`/AOT steps | adapter 选择、System/Store 注册、配置、Persist 声明 |  | 内容组装、导航政策、玩法、字体/HTTP 等 feature 行为 |

### Store 所有权

| Store | 数据聚合/子树 | 唯一写入者 | 读取者 | 持久化 | 事件 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

### System 生命周期

| System/接口 | 注册实现 | 初始化前置 | 生命周期责任 | 关闭行为 |
|---|---|---|---|---|
|  |  |  |  |  |

### Procedure 与提交点

| Procedure | 触发者 | 读取 | 写入/调用 | 提交点 | 成功/失败事件 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

### View 与意图方向

| View | 读取投影 | 可发起意图 | 禁止责任 |
|---|---|---|---|
|  |  |  | 不直接拥有或修改可恢复领域状态 |

给出小型调用图，至少表现 `View → Procedure → Store/System → Event → View` 和组合根注册顺序。

## 现状到目标拓扑差异

| 现有文件 | 当前可验证行为 | 目标行为 | 必须修改的输出/拥有者 | 验证证据 |
|---|---|---|---|---|
|  |  |  |  |  |

目标顺序不能只画在图里。凡是现有代码与目标不同，都必须列出真实修改文件和测试/编译证据。

## 目录、命名空间与程序集

### 目录基线与差异

| 类别 | 目标工程/采纳参考基线 | 本项目选择 | 差异理由 |
|---|---|---|---|
| 能力代码 | `Assets/Game/Scripts/Modules/<Capability>` |  |  |
| Window/View | 目标工程既有约定 |  |  |
| Editor/Generated/Tests | 目标工程既有约定 |  |  |

能力目录优先内聚 Store/System/Procedure/Data/Events/Internal。不得默认创建 `Runtime/<Layer>`、`Presentation/<Feature>` 的双树结构。

### Assembly budget

- 目标工程现有产品 asmdef：
- 参考项目采纳基线：
- 本项目计划新增数量：
- 预算理由：每个新增 asmdef 必须对应 forbidden dependency、平台编译约束或明确的编译隔离收益。

| Assembly | existing/new | 根命名空间 | 覆盖目录 | 允许依赖 | 禁止依赖 | 必要性证据 |
|---|---|---|---|---|---|---|
|  |  |  |  |  |  |  |

asmdef 是编译/依赖 seam，不是概念模块。先定义最小拓扑，再在模块文档列文件；不要为了测试或概念分层随意暴露内部 seam。

### Assembly 物理归属

| asmdef 文件路径 | 最近祖先 source root | 被嵌套 asmdef/asmref 排除的目录 | 代表源码文件→实际 Assembly | 依赖方向/环检查 |
|---|---|---|---|---|
|  |  |  |  | pass/fail |

同目录的两个 asmdef 不能按文件名划分后代源码。每个输出文件声明的 Assembly 必须与最近祖先 asmdef 一致。

## 依赖关系

用小型 ASCII 图表示生产者 → 消费者，不要按文件夹层级作图。

## 波次计划

每个波次说明可观察结果、包含模块、可并行项和进入下一波的 Gate。

| 波次 | 模块 | 可观察结果 | Gate Evidence |
|---|---|---|---|
| W1 |  |  |  |

## 接口对齐

| Interface/事件/投影 | 生产模块 | 消费模块 | 输入/输出一致 | 错误语义一致 | 测试 seam |
|---|---|---|---|---|---|
|  |  |  | pass/fail | pass/fail |  |

## 配置与资源

- Luban 源配置：
- 非代码资源：数量少且所有权简单时直接列在本节；存在多个资源族、导入规则、构建路径或独立 Gate 时，将顶部“资源清单”设为 `资源清单.md` 并使用独立清单。
- 可选 July 包与选择理由：

## GDD 所有权覆盖

| GDD 子系统/行为 | 唯一拥有模块 | 协作模块 | 验收落点 |
|---|---|---|---|
|  |  |  |  |

每个子系统必须有且只有一个所有者。协作模块不能复制其规则或状态。

## 架构审核

- [ ] 复杂度、模块粒度、能力所有权和 Interface 通过审核
- [ ] 精确调用证据与框架充足性通过审核，不存在产品 workaround
- [ ] Store/System/Procedure/View 的权威、生命周期、提交点与意图方向一致
- [ ] 当前到目标的修改、目录、Assembly 物理归属和依赖方向可实现
- [ ] locality、adapter/seam 和组合根责任通过审核
- [ ] 每项 GDD 行为均有唯一所有者和可执行验收证据

## 变更记录

| 版本 | 日期 | 变更 | GDD 版本 |
|---|---|---|---|
| v1.0 | YYYY-MM-DD | 初版 | vX.Y |
