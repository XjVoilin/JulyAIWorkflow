# M{N}_{模块名}

> 版本：v1.0 | 日期：YYYY-MM-DD | 状态：draft
> 上游：`MDD/索引.md` vX.Y，`GDD.md` vX.Y

## 责任

用一段话说明模块拥有的行为，以及明确不拥有的行为。

### cohesion 证明

- 共同变化原因：
- 删除测试：删除本模块后，哪些复杂度会散落到哪些调用者？
- 明确拆出的相邻能力：

### locality 证明

- 能力根目录：`Assets/Game/Scripts/Modules/<Capability>` 或目标工程既有等价目录
- Store/System/Procedure/Data/Events/Internal 的共置关系：
- 必须落在能力目录外的 Window/Editor/Generated/Resource 文件及原因：
- 典型变化场景及预计修改目录：

## Interface

### 调用者与操作

| 调用者 | 操作 | 参数/前置 | 返回/事件 | 复杂度与性能 |
|---|---|---|---|---|
|  |  |  |  |  |

### 输入

| 输入 | 来源 | 不变量 |
|---|---|---|
|  |  |  |

### 输出

| 输出 | 消费方 | 可观察语义 |
|---|---|---|
|  |  |  |

### 错误与顺序

列出调用方必须知道的错误模式、生命周期、顺序和性能特性。内部实现细节不属于 Interface。

## 状态与数据所有权

| 数据/状态 | 权威所有者 | 允许修改者 | 生命周期 | 持久化/配置来源 |
|---|---|---|---|---|
|  |  |  |  |  |

- Store 的一致性边界：说明为何这些字段必须原子变化。
- mutation 入口：列出 Store 内部领域方法；不得只暴露“替换任意根数据”的通用入口给多个模块。

## JulyArch 组合

| 角色 | 具体类型 | 注册/创建位置 | 使用能力 | 不承担的责任 |
|---|---|---|---|---|
| Store/System/Procedure/View/普通类 |  |  |  |  |

- 注册与初始化顺序：
- 调用方向：
- 事件订阅与释放：

## 精确调用证据

| 调用目的 | package@pin/目标文件 | 完整签名或枚举成员 | 调用时序 | 成功结果 | 失败/恢复结果及产品处理 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

若失败只写日志、返回默认值或无法与“无数据”区分，本模块不得把能力标为 READY；先进入框架 Gate。
本节是模块专属调用证据的事实源；索引只保存组合根或跨模块调用，不重复本表签名。

## 依赖与 seam

| 依赖 | 类别 | seam | adapter/测试替身 | 选择理由 |
|---|---|---|---|---|
|  | in-process/local-substitutable/remote-owned/true-external |  |  |  |

只有真实变化或测试替身存在时才建立 adapter。模块内部 seam 不进入外部 Interface。

## 实现约束

- July 能力与已验证接口：
- Luban 源表与生成入口：
- 线程/异步/生命周期：
- 平台限制：
- 禁止事项：

## 输出文件

| 文件 | Assembly/命名空间 | 类型 | 责任 |
|---|---|---|---|
|  |  | 新建/修改/生成 |  |

输出文件应优先聚集在能力根目录。若一个普通能力变更需要同时修改三个以上产品能力目录，重新检查模块深度与 seam。

## 验收证据

| 条件 | 验证方式 | 预期证据 |
|---|---|---|
|  |  |  |

## 依赖就绪检查

- [ ] 所有生产者接口存在并与索引一致
- [ ] 所需配置源、资源和平台能力已就绪
- [ ] 本模块没有依赖后续波次
- [ ] 具体 July 成员均有精确 pin 或目标工程源码证据
- [ ] GDD 要求的缺失、损坏、取消、保存/恢复失败均有消费者可观察通道
- [ ] Store/System/Procedure/View 的角色、注册和调用方向与索引一致
- [ ] 外部 Interface 小于实现，测试只跨该 Interface
- [ ] capability 代码按目标工程惯例共置，普通变化不会跨三个以上能力目录
- [ ] 新增 asmdef 是必要编译 seam，而不是为了复刻概念层
- [ ] 每个输出文件声明的 Assembly 与其最近祖先 asmdef 物理归属一致
- [ ] composition/registrar 变更只包含选择、配置、注册或 Persist 声明
